<?php
global $wpdb, $limit, $offset, $tableName, $table, $table_cols;
// // ---------- テーブル ----------
// $tableName = esc_attr($_GET["table"]);
// $table_cols = $wpdb->get_col("DESC {$tableName}", 0);